{
  "name": "gaslamar-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:vendor": "node scripts/vendor.js",
    "build:js": "node scripts/build.js",
    "build": "npm run build:vendor && npm run build:js",
    "dev": "node scripts/build.js --watch"
  },
  "dependencies": {
    "docx": "8.5.0",
    "jspdf": "2.5.1",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
